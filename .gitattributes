# Normalize line endings to LF in the repo; check out native on each OS.
# Stops the constant "LF will be replaced by CRLF" churn on Windows.
* text=auto eol=lf

# Binary files — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.apk binary
*.keystore binary
*.jar binary
